version: '3.8'
services:
  db:
    image: postgres:15
    container_name: poc-pacientes-db
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=123
      - POSTGRES_DB=sobrevidas_db
    ports:
      - "8080:8080"